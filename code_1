Informatica Frontier Certification Workflow is impacted as the email generation logic now filters out inactive users, ensuring only Active users are included in notifications.


CDI-PC Configuration Files are impacted through the removal of outdated INFA_HOME references and the addition of new truststore parameters, altering how the CDI-PC client authenticates and connects to PROD domains.


IMPACS Processing Workflow is impacted by the decommissioning of S76 files, requiring updates so that only S71 files are validated, merged, and processed.


Autosys Jobs and Merge Script are impacted because the revised script now handles only S71 file checks and merge logic, changing the job’s expected file inputs and processing behavior.

